package dependencies

object Modules {

    object Toggle {
        const val SERVICE = ":toggle:toggle-service"
        const val CORE = ":toggle:toggle-core"
        const val RUNNER = ":toggle:toggle-runner"
    }
}
